# Google Cloud MCP Environment Variables Configuration (v1.3)

# --- GOOGLE OAUTH CREDENTIALS ---
# Option 1: Direct Client ID and Secret (No JSON file required for auth)
GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-client-secret

# Option 2: Path to Credentials JSON file (Fallback)
GOOGLE_CREDENTIALS_PATH=credentials.json

# --- GOOGLE AUTHENTICATION TOKEN ---
# Option 1: Direct Token JSON String (Zero File Mode!)
# After initial login, copy the entire content of .token.json and paste it here.
# GOOGLE_TOKEN_JSON={"token": "...", "refresh_token": "...", ...}

# Option 2: Path to Token file (Defaults to .token.json)
# GOOGLE_TOKEN_PATH=.token.json

# --- INITIAL SETUP GUIDE ---
# 1. Provide GOOGLE_CLIENT_ID and GOOGLE_CLIENT_SECRET.
# 2. Run the server: uv run server.py
# 3. Complete the OAuth flow via the provided URL in the console.
# 4. A '.token.json' file will be created automatically.
# 5. (PRO TIP) Copy the content of '.token.json' to GOOGLE_TOKEN_JSON in your production environment to run without local files.
